{
  "name": "advent-calendar",
  "version": "1.0.0",
  "description": "A secure advent calendar web application",
  "scripts": {
    "start": "firebase emulators:start",
    "logs": "firebase functions:log",
    "deployf": "firebase deploy",
    "deploy": "git add . && git commit -m \"Deploy update $(date '+%Y-%m-%d %H:%M:%S')\" && git push && npm run deployf"
  },
  "dependencies": {
    "firebase-tools": "^13.0.2"
  }
}
